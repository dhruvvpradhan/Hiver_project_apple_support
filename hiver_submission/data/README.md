# Data

Do not commit the full TWCS dataset.

Use a documented subsample and produce a paired CSV with:
- `text_customer`
- `text_apple`

The evaluation golden set is separate and must not be used for training or retrieval tuning.
